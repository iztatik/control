from setuptools import setup

setup(
      name = 'itresp', 
      version = '0.0.1',
      description = 'Iterative response of continuous and discrete linear ssstems',
      author = 'Iztatik',
      packages = ['itresp'],
      zip_safe = False
      )


